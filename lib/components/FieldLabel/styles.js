import styled from "styled-components";
import { Body } from "../../elements/Typography";

const StyledFieldLabel = styled(Body)`
  line-height: 20px;
  margin-top: 0px;
  margin-bottom: 3px;
`;

export { StyledFieldLabel };
